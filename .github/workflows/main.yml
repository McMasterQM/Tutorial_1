Skip to content
Search or jump to…
Pull requests
Issues
Marketplace
Explore
 
@RichRick1 
testTAMcMaster
/
stud_test-ValeriiChuiko
Private
generated from RichRick1/test_TA_1
0
00
Code
Issues
Pull requests
Actions
Projects
Security
Insights
Settings
stud_test-ValeriiChuiko/.github/workflows/testing.yml
@github-classroom
github-classroom Initial commit
Latest commit 48b7d41 on Sep 17
 History
 1 contributor
40 lines (35 sloc)  1.27 KB
   
# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Python application

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.8
      uses: actions/setup-python@v2
      with:
        python-version: 3.8
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install jupyter
        pip install flake8 pytest
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Converting file to py file
      run: |
        python convert.py
    - name: Test with pytest
      run: |
        pytest problem/test/test_Normalizing_eigenfunctions.py
